var assert = require('assert');

describe('Vote', function () {

	it('checks model fields', function () {
		assert.equal([1, 2, 3].indexOf(4), -1);
	});

	it('saves correctly', function () {
			assert.equal([1, 2, 3].indexOf(4), -1);
		});


});